<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No <uses-permission> anywhere in this file, and that is the point.

         KODO's central promise is that it works with the aircraft mode on and sends a
         child's work nowhere. Enforcing that in Dart is a lint; enforcing it here means
         Android itself will not let the process open a socket. INTERNET appears only in
         the debug and profile manifests, where the Flutter tooling needs it to attach a
         debugger, and never in a release build.

         Adding a plugin can add a permission without anyone reading this file, so a test
         reads it instead: app/test/m19_acceptance_test.dart. -->
    <!-- allowBackup is off on purpose. A child's progress, their projects and their
         session are theirs and stay on the device; Android's automatic backup would copy
         the app's own directory into whoever's Google account owns the phone, which is a
         silent export of a child's work to a third party and exactly what NFR-PRIV-01
         says will not happen. The extraction rules say the same for Android 12 and
         later, including device-to-device transfer. -->
    <application
        android:label="KODO"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
