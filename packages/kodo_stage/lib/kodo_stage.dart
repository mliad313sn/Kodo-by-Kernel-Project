/// KODO — M4, the world a program acts on.
///
/// Two surfaces, one turtle model, one contract with M1. Everything here is pure Dart: the
/// Flutter painter that draws a [VectorCanvas] on screen adds pixels, not geometry, so the
/// figure a child watches and the figure the grader judges cannot diverge.
library;

export 'src/consent.dart';
export 'src/inspector.dart';
export 'src/palette.dart';
export 'src/png.dart';
export 'src/raster.dart';
export 'src/stage.dart';
export 'src/svg.dart';
export 'src/vector_canvas.dart';
