/// KODO — M17 and M8: what we measure, and what we reward.
///
/// One package, because they are one argument. §10 commits KODO to instrumenting wellbeing
/// rather than only retention, and a wellbeing metric in a different module from the
/// reward system is a metric nobody reads before shipping a reward.
library;

export 'src/events.dart';
export 'src/item_health.dart';
export 'src/motivation.dart';
